#include<stdio.h>
#include<stdlib.h>
typedef struct admin {
	char username[10];
	char password[10];
	int idno;
	} admin;
typedef struct traveller {
	char username[10];
	char password[10];
	} traveller;
